# Retired: achievement badges removed from the profile README (declutter).
# Kept as a manual-only stub so historical workflow runs remain discoverable.
# Do not re-enable on a schedule without restoring the README badge section.
name: my-badges (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Badge automation retired
        run: |
          echo "my-badges profile section and scheduled updates are retired."
          echo "Historical badge files remain under my-badges/."
          exit 0
